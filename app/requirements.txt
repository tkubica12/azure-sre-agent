# Pinned runtime dependencies for the PulseMart demo workload.
#
# Bump deliberately and re-run app/tests before deploying (see AGENTS.md
# "Infrastructure as code" / pinning philosophy, applied to the app runtime).
fastapi==0.140.13
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
azure-monitor-opentelemetry==1.8.9
python-json-logger==4.1.0
