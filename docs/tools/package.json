{
  "name": "azure-sre-agent-docs-devtools",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only offline validation checks for docs/ (slides + guide). Not part of the shipped, vendored deck itself - jsdom is a checking dependency only, never loaded by index.html.",
  "type": "commonjs",
  "scripts": {
    "validate": "node validate.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
